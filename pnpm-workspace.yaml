packages:
  - 'packages/*'
  - 'apps/*'

# pnpm 11 requires postinstall scripts to be opted into explicitly. esbuild (via Vite and
# Vitest) needs its to unpack the platform binary.
allowBuilds:
  esbuild: true
